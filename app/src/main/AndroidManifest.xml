<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AulaclickApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DashboardActivity" />
        <activity android:name=".DetalleRecursoActivity" />
        <activity android:name=".NuevaReservaActivity" />
        <activity android:name=".MisReservasActivity" />
        <activity android:name=".AdminPanelActivity" />
        <activity android:name=".AnadirRecursoActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".GestionDepartamentosActivity"/>
        <activity android:name=".GestionTiposActivity"/>
        <activity android:name=".GestionEquipamientoActivity"/>

    </application>

</manifest>
